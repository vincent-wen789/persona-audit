# LOCAL — 你的 persona-audit 私有绑定(模板)

> 复制成 `LOCAL.md` 再填(`LOCAL.md` 已 .gitignore,不进开源)。
> 这是引擎模式 5 个私有钩子的填实处。不填也能跑——每个钩子都有缺省降级（见 `references/engine-mode.md`）。

## 钩子填实

| 钩子 | 填你的值 | 不填时的降级 |
|------|----------|--------------|
| 设计宪法 | 例：`<repo>/docs/copy-constitution.md`（C 层判定依据） | 缺 → 只把确定硬伤放 A |
| 存档位置 | 例：`<product-repo>/reviews/`，文件名 `<product>-persona-audit-<date>.md` | 缺 → 存产品 repo 根 |
| 样本生成 | **把真实可跑命令写死在这**。例：`cd <repo> && DEMO_MODE=1 python gen_report.py --branch all`；或“从生产 DB 导最近 8 封周报并脱敏” | 见 `references/engine-mode.md` 的三档降级 |
| 修改后验证 | 产品测试、真实输出和禁词命令；只在用户明确要求应用修复后使用 | 至少运行产品现有测试 |
| 发布流程 | 单端/双端同步或部署命令；**只在用户单独授权发布后参考** | 不发布 |

## 画像取数规则(填你产品的真实值)

- **画像① 镜像**:你产品的真实主用户是谁?身份/术语水平/使用场景/时段/设备——照实写一段,沿用最近一次存档审计的身份块。
- **画像③ 资深**:你这域的 power user 是谁(无"从业者"概念的域 = 资深编辑 / 十年老用户 / 数据分析师……)。
- **画像④ 目标用户**:看 SKILL.md 步骤 0 确认的"本次 ship 那层"是给谁做的。

## 修改后验证

- 禁词 grep = 你的 ship-checklist 里的工程词清单（没有就跳过）。
- 测试套件 = `<你的 test 命令>`。
- 真实输出验证 = `<生成一份正常态和一份边缘态的命令>`。
- 审计本身不运行这一节；只有用户明确要求应用修复后才运行。

## B/C follow-up 去向

例:GitHub issue / TODO 文件 / handoff。

## 验证记录(可选,记你自己的战绩)

- `<product> <date>`:N 条候选 → M 条采纳 → 路线图/修复。
